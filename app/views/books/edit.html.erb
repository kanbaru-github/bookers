

　<% if @book.errors.any? %>
    error
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
    <% end %>
  <% end %>

<h1>Editing</h1>

<%= form_with model:@book, local: true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %><br>

  <%= f.submit 'Update Book', class:"block" %><br>

  <%= link_to "Show", book_path %>

  |

  <%= link_to "Back", books_path %>

<% end %>